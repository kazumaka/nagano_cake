<div class="container">
  <label class="mt-4 mb-4 lead bg-light">ジャンル編集</label>

  <%= form_with model: @genre, url: admin_genre_path(@genre) do |f| %>
    <label class="mr-5">ジャンル名</label>
    <%= f.text_field:name %>
    <%= f.submit "変更を保存",class: "btn btn-success" %>
  <% end %>
</div>